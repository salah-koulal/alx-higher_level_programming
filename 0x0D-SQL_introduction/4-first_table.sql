-- script that creates a table called first_table in the current database in my MySQL server.
-- Using CREATE and an IF condition
CREATE TABLE IF NOT EXISTS first_table (
    id int,
    name VARCHAR(256)
);
